use pyo3::prelude::*;
use pyo3::wrap_pyfunction;

// use crate::view::__pyo3_get_function_nearest_indices_from_coordinates_noddy;

// #[pymodule]
// fn flanders(_py: Python, m: &PyModule) -> PyResult<()> {
//     m.add("__version__", env!("CARGO_PKG_VERSION"))?;

//     m.add_function(wrap_pyfunction!(nearest_indices_from_coordinates_noddy, m)?)?;

//     Ok(())
// }
